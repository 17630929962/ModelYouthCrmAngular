import { OnDestroy, ChangeDetectorRef, AfterViewInit, OnChanges, SimpleChanges, NgZone, SimpleChange } from '@angular/core';
import { ControlValueAccessor } from '@angular/forms';
import { SimplemdeConfig } from './config';
export declare class SimplemdeComponent implements AfterViewInit, OnChanges, OnDestroy, ControlValueAccessor {
    private cog;
    private cd;
    private zone;
    private con;
    private instance;
    private value;
    private onChange;
    private onTouched;
    options: any;
    /** 风格，默认：`antd` */
    style: 'default' | 'antd';
    /** 延迟初始化 */
    delay: number;
    constructor(cog: SimplemdeConfig, cd: ChangeDetectorRef, zone: NgZone);
    private initDelay();
    private init();
    private destroy();
    ngAfterViewInit(): void;
    ngOnChanges(changes: {
        [P in keyof this]?: SimpleChange;
    } & SimpleChanges): void;
    /**
     * 获取UE实例
     *
     * @readonly
     */
    readonly Instance: any;
    ngOnDestroy(): void;
    _onReuseInit(): void;
    writeValue(value: string): void;
    registerOnChange(fn: (_: any) => {}): void;
    registerOnTouched(fn: () => {}): void;
    setDisabledState(isDisabled: boolean): void;
}
